{% extends 'layout.html.twig' %}

{% block content %}
    <h1>ADMIN _ Modifica Prodotto</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Salva</button>
    <button class="btn btn-default btn-back">Indietro</button>
    {{ form_end(form) }}
    
{% endblock %}